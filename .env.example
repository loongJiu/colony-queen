# 服务配置
PORT=8001
HOST=0.0.0.0
NODE_ENV=development

# 安全
QUEEN_SECRET=change-me-in-production
COLONY_TOKEN=change-me-in-production

# 调度配置
SCHEDULER_MAX_RETRY=3
SCHEDULER_RETRY_BASE_DELAY_MS=1000
SCHEDULER_RETRY_MAX_DELAY_MS=30000

# 心跳配置
HEARTBEAT_CHECK_INTERVAL_MS=10000
HEARTBEAT_TIMEOUT_MS=30000

# 任务配置
TASK_DEFAULT_TIMEOUT_S=30
TASK_QUEUE_MAX_SIZE=1000
